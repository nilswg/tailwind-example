const style = 'text-red-50';

const card1 = 'nil-card';

<div className="absolute top-1 nil-red"></div>;

const tabSize = [1, 2, 3, 4, 5].map((i) => `tab-1`);

for (const i of [1, 2, 3, 4, 5]) {
  console.log(i);
}
